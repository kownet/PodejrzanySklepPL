APP.home = (function () {

    var checkStore = function (opts) {

        console.log(opts);

    };

    return {
        checkStore: function (opts) {
            checkStore(opts);
        }
    };

})();